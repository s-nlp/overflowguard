# core — overflowguard package
torch>=2.0
transformers==4.57.6
numpy>=1.24
scikit-learn>=1.3
huggingface_hub>=0.20
rich>=13.0
pyyaml>=6.0

# LLM judge (overflowguard.evaluate.llm_judge)
openai>=1.0

# demo (demo/app.py) — 1.51 is the first release with the [theme.light] /
# [theme.dark] config sections the demo's theme relies on; tested on 1.58
streamlit>=1.51
requests>=2.28
beautifulsoup4>=4.12
ddgs>=6.0
jinja2>=3.0
